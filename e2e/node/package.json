{
	"name": "@flare/e2e-node",
	"version": "0.0.1",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "bun run dev:kill && bun run --filter @flare/e2e-app dev",
		"dev:kill": "lsof -ti:4101,4102 2>/dev/null | xargs -r kill -9 2>/dev/null || true",
		"test": "bun run dev:kill && playwright test",
		"test:firefox": "bun run dev:kill && playwright test --config playwright.firefox.config.ts",
		"test:prod": "bun run dev:kill && TEST_MODE=prod playwright test",
		"type:check": "tsc --noEmit"
	},
	"dependencies": {
		"@flare/e2e-app": "workspace:*",
		"flare": "workspace:*"
	},
	"devDependencies": {
		"@axe-core/playwright": "catalog:",
		"@playwright/test": "catalog:",
		"@types/node": "catalog:",
		"typescript": "catalog:"
	}
}
